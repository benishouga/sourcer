import Utils = require('../main/Utils');
import chai = require('chai');

var assert = chai.assert;

describe('Utils', function() {
  describe('#side()', function() {
    it('fire!!', function() {
      assert.ok(true, "Its' ok !");
      assert.ok(true, "Its' ok !!!");
    });
  });
});
